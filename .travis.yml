before_install:
before_script:
script:
  - ./checkout.sh --yes
  - echo $ROBO_PATH_WORKSPACE
  - source bash/roboag/bashrc.sh && echo $ROBO_PATH_WORKSPACE
  - source bash/roboag/bashrc.sh && alias
  - source bash/roboag/bashrc.sh && repo_help
  - source bash/roboag/bashrc.sh && repo_clone_all --yes

deploy:
